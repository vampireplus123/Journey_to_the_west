# Bắt đầu bằng việc bỏ qua (ignore) tất cả mọi thứ trong thư mục dự án.
*

# Nhưng không bỏ qua các thư mục, để Git có thể quét vào bên trong chúng.
!*/

# Cho phép (un-ignore) các loại file mã nguồn bạn muốn theo dõi.
!*.cs
!*.cpp
!*.h

# QUAN TRỌNG: Cho phép file .uproject để có thể mở dự án.
!*.uproject

# Đề xuất: Cho phép các file cấu hình trong thư mục Config.
# Đây là những file text nhỏ chứa setting quan trọng của game.
!/Config/
!/Config/*.ini

# Cuối cùng, đừng bỏ qua chính file .gitignore này.
!.gitignore
